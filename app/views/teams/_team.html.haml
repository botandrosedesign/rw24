%tr{ :class => [team.legacy_category_abbrev_with_gender, team.legacy_category_abbrev].uniq.join(" ") }
  %td= fixed_number_or_empty team.position, :size => 3
  %td
    - if @race.year > 2009
      = team.legacy_category_abbrev_with_gender
    - else
      = team.legacy_category_abbrev
  %td.team= link_to_if @race.year > 2009, team.name, leader_board_laps_path(:year => @race.year, :position => team.position)
  %td= fixed_number_or_empty team.laps_total
  %td= fixed_number_or_empty team.laps_total * 4.6
  %td= fixed_number_or_empty team.bonuses_total
  - if @race.year > 2009
    %td= fixed_number_or_empty team.penalties_total
  %td
    %b= fixed_number_or_empty team.points_total
