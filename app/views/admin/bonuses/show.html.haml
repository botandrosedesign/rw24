%h2 Bonus ##{@bonus.id} - #{@bonus.name}
%table#bonus-view
  - @bonuses.each do |bonus|
    %tr
      %th= bonus.team_position
      %td= bonus.team_name
      %td= link_to "Delete", point_path(bonus), :class => "delete", :method => :delete, :confirm => "Are you sure?"
